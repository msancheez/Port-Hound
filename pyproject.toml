[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "port-hound"
version = "1.0.0"
description = "TCP Port Scann"
authors = [{name = "msancheez"}]
dependencies = []

[project.scripts]
porthound = "scanner.cli:main"